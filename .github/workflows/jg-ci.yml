name: jg CI

on:
  push:
    paths:
      - 'apps/jg/**'
      - '.mise.toml'
      - '.github/workflows/jg-ci.yml'
  pull_request:
    paths:
      - 'apps/jg/**'
      - '.mise.toml'
      - '.github/workflows/jg-ci.yml'

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/jg
    steps:
      - uses: actions/checkout@v6
      - uses: jdx/mise-action@v4
        with:
          working_directory: apps/jg
      - run: mise run fmt-check
      - run: mise run lint
      - run: mise run test
      - run: mise run build
