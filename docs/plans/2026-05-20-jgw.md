# jgw 구현 plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `jg` 의 worktree 모드 형제 CLI 인 `jgw` 를 추가하고, 그 첫 사례로 저장소에 "자동완성 최대 제공" 원칙을 도입한다. 동시에 `jg` 와 `jgw` 의 frecency store 를 XDG Base Directory 표준 경로로 옮겨 같은 폴더에 보관한다.

**Architecture:** `apps/jg/` 단일 바이너리에 argv0 기반 분기를 추가한다. Homebrew formula 가 `jgw` 심볼릭 링크를 함께 설치한다. frecency store 는 `${XDG_STATE_HOME:-~/.local/state}/jg/` 디렉토리 아래에 repo 점수 파일(`repos`) 과 worktree 점수 파일(`worktrees`) 두 개로 둔다. `jg` 의 기존 데이터 파일(`~/.jg`) 은 자동 마이그레이션한다.

**Tech Stack:** Go 1.23, fzf, git, GoReleaser, Homebrew tap.

---

## 1. 설계 컨텍스트와 영구 근거

본 섹션은 모든 task 가 일관된 판단으로 진행되도록, 설계 결정과 그 근거를 한곳에 모아 둔다. 본 plan 한 파일만 읽어도 의도가 복원되도록 자체완결성을 유지한다.

### 1.1 busybox 패턴으로 `jg` 와 `jgw` 를 한 바이너리에 둔다

본 저장소의 모노레포 컨벤션은 "한 도구 = `apps/<tool>/` 한 디렉토리, 도구 사이 코드 공유 금지" 다. `jgw` 가 `jg` frecency store 를 읽어야 하는 이상 두 도구는 코드 또는 데이터 contract 를 공유할 수밖에 없다. 별 디렉토리로 분리하면 공유 패키지를 만들어야 하고 이는 컨벤션과 정면으로 충돌한다. busybox 패턴은 두 도구를 한 디렉토리 한 바이너리로 묶어 컨벤션을 자연스레 충족시키며, 빌드·CI·릴리스·버전 관리 모두 `jg` 한 도구 단위로만 유지하면 된다. argv0 분기는 UNIX 의 표준 관용구이므로 가독성 비용도 크지 않다.

### 1.2 frecency store 분리 + XDG 동시 마이그레이션

`jg` 가 보유하는 repo 점수와 `jgw` 가 보유하는 worktree 점수는 별 파일로 분리한다. `jg` 점프 동작은 repo 파일만 가산하고, `jgw` 점프 동작은 worktree 파일과 repo 파일 양쪽을 가산한다. 이로써 `jg` 만 쓰는 사용자에게는 `jgw` 도입이 데이터 파일 형태 변화로 보이지 않는다.

같은 작업에서 두 파일 모두 XDG Base Directory 표준 경로(`${XDG_STATE_HOME:-~/.local/state}/jg/`) 아래로 옮긴다. frecency 데이터는 "프로그램이 재시작 사이에 보존하고 싶지만 사용자가 수동 삭제해도 치명적이지 않은 상태" 이므로 XDG 분류상 STATE 에 해당한다. 기존 `~/.jg` 파일은 자동 마이그레이션한다: 새 경로에 파일이 없고 옛 파일이 있으면 옛 파일을 읽어 새 경로로 옮기는 형태다. 옛 파일은 마이그레이션 후 그대로 둔다(사용자가 수동 삭제 가능). 이는 본 저장소 CLAUDE.md 의 "**설정·상태·캐시 파일은 XDG Base Directory 표준을 따른다**" 원칙에 부합한다.

### 1.3 사용자 흐름의 자동 분기

`jgw` 를 인자 없이 호출하면 cwd 를 기준으로 두 흐름 중 하나로 자동 분기한다. cwd 가 git repo 안이면 흐름 a(현 repo 의 worktree 한 단계 picker), 밖이거나 `jgw <pattern>` 으로 명시 호출하면 흐름 b(repo picker → worktree picker 두 단계) 다. 선택된 repo 에 linked worktree 가 0개면 picker 를 띄우지 않고 즉시 원본으로 점프한다.

### 1.4 picker 표시 규약

picker 의 후보 라인 자체는 path 한 줄짜리로만 채운다. 후보 라인에는 브랜치 이름·원본 마커·접두 기호를 두지 않는다. 컨텍스트 정보는 fzf 의 주변 영역으로 분산한다. 상단 header(multi-line) 에 단계 표시와 원본 path 를 둔다. 사용자가 현재 위치한 worktree 가 후보군에 있으면 `--header-lines=1` 로 picker 맨 위에 비활성 행으로 노출한다 — fzf 가 그 행을 검색·선택 대상에서 자동 제외하므로 "보이지만 선택 불가" 가 트릭 없이 성립한다. focused 항목의 브랜치·마지막 커밋 제목·마지막 커밋 시각은 preview 창에 표시한다.

### 1.5 셸 통합

기존 `jg init zsh` / `jg init bash` 출력을 확장해 `jg()` 와 `jgw()` 두 셸 함수를 한 번의 `eval` 로 등록한다. 사용자가 별 `jgw` alias 나 plugin 을 가지고 있을 가능성은 본 plan 에서 특별 처리하지 않는다 — 현재 `jg()` 함수가 동일한 정책으로 emit 되고 있으므로 일관성을 유지한다.

### 1.6 자동완성

`apps/jg/completions/` 아래 `_jg`, `_jgw`, `jg.bash`, `jgw.bash` 4개 파일을 둔다. GoReleaser archive 가 tar.gz 에 담고, Homebrew formula 가 zsh/bash completion 디렉토리에 설치한다.

### 1.7 버전 출력

두 도구는 동일 버전 번호를 공유한다. `versionLine(name, version)` 순수 함수가 한 줄을 만들고, argv0 분기로 얻은 도구 이름을 name 슬롯에 넘긴다.

---

## 2. 수용 기준

본 plan 이 구현되어 다음을 모두 만족할 때 작업이 완료된 것으로 본다.

- cwd 가 git repo 안에서 `jgw` 를 실행하면 그 repo 의 원본과 linked worktree 들이 fzf picker 에 path 한 줄짜리로 나열되고, 사용자가 항목을 고르면 셸 함수가 그 경로로 cd 한다.
- cwd 가 git repo 밖에서 `jgw` 를 실행하면 jg frecency store 의 repo 목록이 1단계 picker 로 뜨고, repo 선택 후 그 repo 의 worktree picker 가 2단계로 이어진다. 각 단계에 단계 표시 헤더가 보인다.
- 선택된 repo 에 linked worktree 가 0개면 picker 를 띄우지 않고 즉시 원본으로 점프한다.
- 현재 위치한 worktree 가 후보군에 포함되면 picker 맨 위 비활성 행에 표시되며, 그 행은 검색·선택 모두 불가능하다.
- focused 항목 변경 시 preview 창에 그 worktree 의 브랜치, 마지막 커밋 제목, 마지막 커밋 시각이 나타난다.
- `jgw` 점프가 worktree store 와 repo store 양쪽 점수를 동시에 올린다. `jg` 점프는 repo store 만 올린다.
- `jg init zsh` 한 줄로 `jg` 와 `jgw` 셸 함수가 모두 정의된다.
- `jg -v` 와 `jgw -v` 가 본 저장소의 version-conformance 형식을 만족한다.
- zsh 와 bash 양쪽에서 `jg`, `jgw` 자동완성이 동작한다.
- frecency store 가 XDG 표준 경로(`${XDG_STATE_HOME:-~/.local/state}/jg/`) 에 자리 잡고, 기존 `~/.jg` 사용자가 자동 마이그레이션된다.
- 루트 `CLAUDE.md` 에 "자동완성 최대 제공" 원칙이 4번째 항목으로 추가되고, 신규 도구 체크리스트에도 자동완성 항목이 등장한다.

---

## 3. 작업 단위

각 task 는 별 commit 으로 끊는다. TDD 사이클(Red → Green → Refactor) 을 따르고, 실패·통과 숫자 쌍을 commit 메시지나 PR 본문에 기재한다.

### Task 1: 루트 CLAUDE.md 에 자동완성 원칙 추가

**Files:**
- Modify: `CLAUDE.md` ("저장소 원칙" 섹션 끝, "새 도구 추가 체크리스트" 섹션)

- [ ] **Step 1: CLAUDE.md 의 저장소 원칙 4번째 항목 추가**

`CLAUDE.md` 의 "저장소 원칙" 섹션의 마지막 줄(`옵션 비교 시 이 세 원칙을 가산점 기준으로 사용한다.`) 직전 위치에 다음 항목을 추가한다.

```markdown
- **자동완성 최대 제공**: 각 도구는 사용자 셸 환경에서 가능한 모든 입력 지점에 자동완성을 제공한다. 정적 후보(subcommand, 플래그) 는 물론 동적 후보(파일/디렉토리/도구 자체가 보유한 항목명) 도 셸이 표현할 수 있는 한 함께 보완한다.
```

그리고 마지막 줄을 다음과 같이 갱신해 4원칙 모두 가산점에 편입한다.

```markdown
옵션 비교 시 이 네 원칙을 가산점 기준으로 사용한다.
```

- [ ] **Step 2: "새 도구 추가 체크리스트" 에 자동완성 항목 추가**

`CLAUDE.md` 의 체크리스트 1번 항목("`apps/<tool>/` 생성, 도구 코드 + ...") 의 파일 목록에 `_<tool>` / `<tool>.bash` 자동완성을 추가하도록 문구를 보강한다. 구체 변경:

기존:
```markdown
1. `apps/<tool>/` 생성, 도구 코드 + `.mise.toml` + README + `.goreleaser.yaml` 추가
```

변경 후:
```markdown
1. `apps/<tool>/` 생성, 도구 코드 + `.mise.toml` + README + `.goreleaser.yaml` + `completions/_<tool>` + `completions/<tool>.bash` 추가
```

- [ ] **Step 3: 검증 — 파일 변경 확인**

```bash
grep -n "자동완성 최대 제공" CLAUDE.md
grep -n "네 원칙" CLAUDE.md
grep -n "completions/_" CLAUDE.md
```

기대: 세 명령 모두 일치 라인 출력.

- [ ] **Step 4: commit**

```bash
git add CLAUDE.md
git commit -m "$(cat <<'EOF'
docs(claude): add autocomplete-maximization repo principle

저장소 4번째 원칙으로 "자동완성 최대 제공" 을 추가하고 신규 도구 체크리스트에도 자동완성
파일 작성을 포함한다. jg/jgw 자동완성을 첫 적용 사례로 둘 예정.
EOF
)"
```

---

### Task 2: XDG 경로 helper 와 entry 마이그레이션

**Files:**
- Create: `apps/jg/internal/xdgpath/xdgpath.go`
- Create: `apps/jg/internal/xdgpath/xdgpath_test.go`
- Modify: `apps/jg/internal/entry/entry.go` (`init()`, `Load()`, `Save()`)
- Modify: `apps/jg/internal/entry/entry_test.go` (마이그레이션 케이스 추가)

- [ ] **Step 1: Red — XDG path helper 테스트 작성**

`apps/jg/internal/xdgpath/xdgpath_test.go` 신규 작성:

```go
package xdgpath

import (
	"os"
	"path/filepath"
	"testing"
)

func TestStateDirUsesXDGStateHome(t *testing.T) {
	t.Setenv("XDG_STATE_HOME", "/tmp/xdg-state")
	t.Setenv("HOME", "/home/test")

	got := StateDir("jg")
	want := "/tmp/xdg-state/jg"
	if got != want {
		t.Errorf("StateDir = %q, want %q", got, want)
	}
}

func TestStateDirFallsBackToHomeLocalState(t *testing.T) {
	t.Setenv("XDG_STATE_HOME", "")
	t.Setenv("HOME", "/home/test")

	got := StateDir("jg")
	want := filepath.Join("/home/test", ".local/state/jg")
	if got != want {
		t.Errorf("StateDir = %q, want %q", got, want)
	}
}

func TestStateDirSkipsEmptyTool(t *testing.T) {
	t.Setenv("XDG_STATE_HOME", "/tmp/xdg-state")
	got := StateDir("")
	if got != "/tmp/xdg-state" {
		t.Errorf("StateDir(\"\") = %q, want %q", got, "/tmp/xdg-state")
	}
	_ = os.Getenv // keep import used in some build modes
}
```

- [ ] **Step 2: Red 실행 — 실패 관찰**

```bash
cd apps/jg && go test ./internal/xdgpath/...
```
기대: `package xdgpath; no Go files` 또는 `undefined: StateDir` 같은 컴파일 실패. **단, TDD 강화 조항대로 빈 시그니처만 두고 실행 실패를 한 번 확인한다.** 빈 구현 작성 후 재실행:

```go
// apps/jg/internal/xdgpath/xdgpath.go (Red 단계 빈 구현)
package xdgpath

func StateDir(tool string) string { return "" }
```

```bash
go test ./internal/xdgpath/... -v
```
기대: 3개 테스트 모두 FAIL. 실패 메시지에 `StateDir = "", want ...` 형태가 보일 것.

- [ ] **Step 3: Green — XDG helper 구현**

`apps/jg/internal/xdgpath/xdgpath.go` 본문 작성:

```go
// Package xdgpath returns XDG Base Directory paths with sensible fallbacks
// when the corresponding environment variables are unset.
package xdgpath

import (
	"os"
	"path/filepath"
)

// StateDir returns the directory where the given tool stores state files.
// It honors XDG_STATE_HOME and falls back to ~/.local/state when unset.
// Passing an empty tool name returns the base state dir without a tool suffix.
func StateDir(tool string) string {
	base := os.Getenv("XDG_STATE_HOME")
	if base == "" {
		home, err := os.UserHomeDir()
		if err != nil {
			return ""
		}
		base = filepath.Join(home, ".local", "state")
	}
	if tool == "" {
		return base
	}
	return filepath.Join(base, tool)
}
```

- [ ] **Step 4: 테스트 실행 — Green 확인**

```bash
go test ./internal/xdgpath/... -v
```
기대: 3개 PASS. 통과 숫자 메모(예: `3 passed, 0 failed`).

- [ ] **Step 5: Red — entry 마이그레이션 테스트 추가**

`apps/jg/internal/entry/entry_test.go` 끝에 다음 테스트를 추가한다.

```go
func TestLoadMigratesFromLegacyDataFile(t *testing.T) {
	legacyDir := t.TempDir()
	xdgDir := t.TempDir()

	legacyPath := filepath.Join(legacyDir, ".jg")
	legacyContent := "/path/to/repo|2.5|1700000000\n"
	if err := os.WriteFile(legacyPath, []byte(legacyContent), 0644); err != nil {
		t.Fatal(err)
	}

	LegacyDataFile = legacyPath
	DataFile = filepath.Join(xdgDir, "repos")

	entries, err := Load()
	if err != nil {
		t.Fatalf("Load failed: %v", err)
	}
	if len(entries) != 1 || entries[0].Path != "/path/to/repo" {
		t.Errorf("Load did not read legacy file: got %+v", entries)
	}
}

func TestSaveWritesToNewDataFile(t *testing.T) {
	dir := t.TempDir()
	DataFile = filepath.Join(dir, "repos")
	LegacyDataFile = filepath.Join(dir, "nonexistent")

	err := Save([]Entry{{Path: "/x", Rank: 1, Timestamp: 1700000000}})
	if err != nil {
		t.Fatalf("Save failed: %v", err)
	}
	if _, err := os.Stat(DataFile); err != nil {
		t.Errorf("new data file not created: %v", err)
	}
}
```

`os` `path/filepath` import 가 이미 있는지 확인하고 없으면 추가한다.

- [ ] **Step 6: Red 실행**

```bash
go test ./internal/entry/... -run "Migrates|WritesToNew" -v
```
기대: `LegacyDataFile` 미정의로 컴파일 실패. 빈 변수 선언 후 재실행해서 실제 동작 실패를 관찰한다:

```go
// entry.go 에 빈 시그니처만 먼저 추가
var LegacyDataFile string
```

다시 실행하면 `TestLoadMigratesFromLegacyDataFile` 가 entries 가 비어 FAIL.

- [ ] **Step 7: Green — entry.go 마이그레이션 로직 구현**

`apps/jg/internal/entry/entry.go` 의 `init()` 와 `Load()` 를 다음과 같이 수정한다.

```go
// 기존 import 에 추가:
//   "github.com/silee-tools/jg/internal/xdgpath"

var (
	DataFile       string
	LegacyDataFile string
)

func init() {
	stateDir := xdgpath.StateDir("jg")
	if stateDir != "" {
		_ = os.MkdirAll(stateDir, 0755)
		DataFile = filepath.Join(stateDir, "repos")
	} else {
		DataFile = filepath.Join(os.TempDir(), "jg-repos")
	}

	if home, err := os.UserHomeDir(); err == nil {
		LegacyDataFile = filepath.Join(home, ".jg")
	}
}

// loadFromPath 는 단일 파일에서 entries 를 읽는다. 공유 helper.
func loadFromPath(path string) ([]Entry, error) {
	f, err := os.Open(path)
	if err != nil {
		if os.IsNotExist(err) {
			return nil, nil
		}
		return nil, err
	}
	defer f.Close()

	if err := syscall.Flock(int(f.Fd()), syscall.LOCK_SH); err != nil {
		return nil, err
	}
	defer syscall.Flock(int(f.Fd()), syscall.LOCK_UN)

	var entries []Entry
	scanner := bufio.NewScanner(f)
	for scanner.Scan() {
		line := strings.TrimSpace(scanner.Text())
		if line == "" {
			continue
		}
		if e, ok := parseLine(line); ok {
			entries = append(entries, e)
		}
	}
	return entries, scanner.Err()
}

func Load() ([]Entry, error) {
	entries, err := loadFromPath(DataFile)
	if err != nil {
		return nil, err
	}
	if entries != nil {
		return entries, nil
	}
	if LegacyDataFile == "" {
		return nil, nil
	}
	legacy, err := loadFromPath(LegacyDataFile)
	if err != nil {
		return nil, err
	}
	return legacy, nil
}
```

- [ ] **Step 8: Green 확인**

```bash
go test ./internal/entry/... -v
```
기대: 모든 기존 + 신규 테스트 PASS. 숫자 메모.

- [ ] **Step 9: Refactor — 기존 `Load` 안의 인라인 로직 정리 확인**

기존 `Load` 본체가 `loadFromPath` 호출로 단순화되었는지, dead code 가 남지 않았는지 점검. 변경 없으면 그대로 둔다.

- [ ] **Step 10: 전체 jg 테스트 실행 — 회귀 확인**

```bash
cd apps/jg && go test ./...
```
기대: 모든 테스트 PASS.

- [ ] **Step 11: commit**

```bash
git add apps/jg/internal/xdgpath apps/jg/internal/entry
git commit -m "$(cat <<'EOF'
refactor(jg)!: move repo store to XDG_STATE_HOME with auto-migration

frecency store 를 ~/.jg 에서 ${XDG_STATE_HOME:-~/.local/state}/jg/repos 로 옮기고,
기존 사용자에게 투명한 자동 마이그레이션(Load 시 신규 파일 없으면 옛 파일 폴백 읽기,
이후 Save 가 신규 경로로 작성) 을 제공한다. 옛 파일은 그대로 둔다.
EOF
)"
```

`feat!` / `refactor!` 의 `!` 표시는 store 경로 변경이 외부에서 보이는 동작 변화임을 나타낸다.

---

### Task 3: argv0 dispatch 골격과 jgw 기본 명령 처리

**Files:**
- Modify: `apps/jg/cmd/jg/main.go` (`main()`, 신규 `runJgw()`)
- Create: `apps/jg/cmd/jg/main_test.go` (이미 존재, 케이스 추가)

- [ ] **Step 1: Red — argv0 분기 테스트 작성**

`apps/jg/cmd/jg/main_test.go` 에 다음을 추가한다.

```go
func TestToolNameFromArgv0(t *testing.T) {
	cases := []struct {
		argv0 string
		want  string
	}{
		{"/usr/local/bin/jg", "jg"},
		{"/opt/homebrew/bin/jgw", "jgw"},
		{"jg", "jg"},
		{"./jgw", "jgw"},
	}
	for _, c := range cases {
		got := toolName(c.argv0)
		if got != c.want {
			t.Errorf("toolName(%q) = %q, want %q", c.argv0, got, c.want)
		}
	}
}
```

- [ ] **Step 2: Red 실행**

```bash
cd apps/jg && go test ./cmd/jg/... -run TestToolNameFromArgv0 -v
```
빈 시그니처 먼저:

```go
func toolName(argv0 string) string { return "" }
```

다시 실행해 실패 메시지 확인.

- [ ] **Step 3: Green — `toolName` 구현 + `main()` 분기 추가**

`apps/jg/cmd/jg/main.go` 수정:

```go
func toolName(argv0 string) string {
	return filepath.Base(argv0)
}

func main() {
	tool := toolName(os.Args[0])
	args := os.Args[1:]

	if tool == "jgw" {
		runJgw(args)
		return
	}

	// 기존 jg 분기 (변경 없음)
	if len(args) == 0 {
		runJump(nil)
		return
	}
	switch args[0] {
	// ... (기존 case 들 그대로)
	}
}

// runJgw 는 jgw 모드의 진입점이다. 추후 task 에서 본체를 채운다.
func runJgw(args []string) {
	if len(args) > 0 {
		switch args[0] {
		case "-v", "--version", "-version":
			fmt.Fprint(os.Stdout, versionLine("jgw", version))
			return
		case "-h", "--help", "-help":
			printJgwHelp()
			return
		}
	}
	fmt.Fprintln(os.Stderr, "jgw: not yet implemented")
	os.Exit(1)
}

func printJgwHelp() {
	fmt.Print(`Usage: jgw [pattern]

Jump to a worktree of the current git repository or pick a repo first.

Behavior:
  jgw                   In a git repo: pick a worktree. Outside: pick a repo, then a worktree.
  jgw <pattern>         Narrow the repo picker by pattern.

Options:
  -v, --version         Show version
  -h, --help            Show this help
`)
}
```

- [ ] **Step 4: 테스트 실행**

```bash
go test ./cmd/jg/... -v
```
기대: PASS. 숫자 메모.

- [ ] **Step 5: 수동 확인 — `jgw -v` 출력 형식**

```bash
go build -o /tmp/jg ./cmd/jg && ln -sf /tmp/jg /tmp/jgw && /tmp/jgw -v
```
기대: `jgw vdev © 2026 silee-tools` (개발 빌드에서는 version=dev).

- [ ] **Step 6: commit**

```bash
git add apps/jg/cmd/jg
git commit -m "$(cat <<'EOF'
feat(jg): add argv0 dispatch and jgw mode skeleton

filepath.Base(os.Args[0]) 가 "jgw" 면 runJgw 로 분기한다. 본 commit 은 골격만
도입하고 -v, -h 만 처리한다. 본체(흐름 a/b, 자동 분기, picker) 는 후속 task 에서 채운다.
EOF
)"
```

---

### Task 4: worktree 발견 모듈

**Files:**
- Create: `apps/jg/internal/worktree/worktree.go`
- Create: `apps/jg/internal/worktree/worktree_test.go`

- [ ] **Step 1: Red — porcelain 파서 테스트 작성**

```go
// apps/jg/internal/worktree/worktree_test.go
package worktree

import (
	"reflect"
	"testing"
)

func TestParsePorcelain(t *testing.T) {
	input := `worktree /Users/x/repo
HEAD abc123
branch refs/heads/main

worktree /Users/x/repo-feature
HEAD def456
branch refs/heads/feature/foo

worktree /Users/x/repo-detached
HEAD ghi789
detached
`
	got, err := parsePorcelain([]byte(input))
	if err != nil {
		t.Fatal(err)
	}
	want := []Worktree{
		{Path: "/Users/x/repo", Branch: "main", IsMain: true},
		{Path: "/Users/x/repo-feature", Branch: "feature/foo", IsMain: false},
		{Path: "/Users/x/repo-detached", Branch: "", IsMain: false},
	}
	if !reflect.DeepEqual(got, want) {
		t.Errorf("parsePorcelain mismatch\n got = %+v\nwant = %+v", got, want)
	}
}
```

- [ ] **Step 2: Red 실행**

빈 시그니처:
```go
package worktree

type Worktree struct {
	Path   string
	Branch string
	IsMain bool
}

func parsePorcelain(out []byte) ([]Worktree, error) { return nil, nil }
```

```bash
go test ./internal/worktree/... -v
```
기대: 1 FAIL.

- [ ] **Step 3: Green — porcelain 파서 구현**

```go
package worktree

import (
	"bufio"
	"bytes"
	"strings"
)

type Worktree struct {
	Path   string
	Branch string
	IsMain bool
}

func parsePorcelain(out []byte) ([]Worktree, error) {
	var result []Worktree
	var cur Worktree
	flush := func() {
		if cur.Path != "" {
			result = append(result, cur)
		}
		cur = Worktree{}
	}
	first := true
	scanner := bufio.NewScanner(bytes.NewReader(out))
	for scanner.Scan() {
		line := scanner.Text()
		if line == "" {
			flush()
			continue
		}
		switch {
		case strings.HasPrefix(line, "worktree "):
			flush()
			cur.Path = strings.TrimPrefix(line, "worktree ")
			cur.IsMain = first
			first = false
		case strings.HasPrefix(line, "branch refs/heads/"):
			cur.Branch = strings.TrimPrefix(line, "branch refs/heads/")
		case line == "detached":
			// branch 는 빈 문자열 유지
		}
	}
	flush()
	return result, scanner.Err()
}
```

- [ ] **Step 4: 테스트 실행 — Green 확인**

```bash
go test ./internal/worktree/... -v
```
기대: PASS.

- [ ] **Step 5: List 함수 추가 (실제 git 호출 wrapper)**

```go
import (
	"os/exec"
)

// List 는 주어진 repo 경로의 worktree 목록을 반환한다.
// repoPath 는 worktree 안의 임의 디렉토리여도 git 이 알아서 main 을 찾는다.
func List(repoPath string) ([]Worktree, error) {
	cmd := exec.Command("git", "-C", repoPath, "worktree", "list", "--porcelain")
	out, err := cmd.Output()
	if err != nil {
		return nil, err
	}
	return parsePorcelain(out)
}
```

이 wrapper 는 실제 git 호출이라 단위 테스트로 검증하지 않는다(테스트는 `parsePorcelain` 으로 충분). 1회성 E2E 에서 검증한다.

- [ ] **Step 6: commit**

```bash
git add apps/jg/internal/worktree
git commit -m "$(cat <<'EOF'
feat(jg): add worktree discovery module

git worktree list --porcelain 출력을 파싱해 (path, branch, isMain) 슬라이스로 반환한다.
파서는 branch refs/heads/<name>, detached, 빈 줄을 모두 처리한다.
EOF
)"
```

---

### Task 5: worktree frecency store

**Files:**
- Create: `apps/jg/internal/wtstore/wtstore.go`
- Create: `apps/jg/internal/wtstore/wtstore_test.go`

- [ ] **Step 1: Red — store I/O 테스트 작성**

```go
// apps/jg/internal/wtstore/wtstore_test.go
package wtstore

import (
	"path/filepath"
	"testing"
)

func TestSaveLoadRoundTrip(t *testing.T) {
	dir := t.TempDir()
	DataFile = filepath.Join(dir, "worktrees")

	want := []Entry{
		{Path: "/repo/wt1", Rank: 2, Timestamp: 1700000000},
		{Path: "/repo/wt2", Rank: 5.5, Timestamp: 1700000100},
	}
	if err := Save(want); err != nil {
		t.Fatal(err)
	}
	got, err := Load()
	if err != nil {
		t.Fatal(err)
	}
	if len(got) != 2 || got[0].Path != "/repo/wt1" || got[1].Rank != 5.5 {
		t.Errorf("round trip mismatch: %+v", got)
	}
}

func TestAddOrUpdateBumpsRank(t *testing.T) {
	dir := t.TempDir()
	DataFile = filepath.Join(dir, "worktrees")

	if err := AddOrUpdate("/repo/wt1"); err != nil {
		t.Fatal(err)
	}
	if err := AddOrUpdate("/repo/wt1"); err != nil {
		t.Fatal(err)
	}
	entries, _ := Load()
	if len(entries) != 1 || entries[0].Rank != 2 {
		t.Errorf("AddOrUpdate did not bump rank: %+v", entries)
	}
}
```

- [ ] **Step 2: Red 실행**

빈 시그니처 작성 후 실행:

```go
package wtstore

var DataFile string

type Entry struct {
	Path      string
	Rank      float64
	Timestamp int64
}

func Save(entries []Entry) error      { return nil }
func Load() ([]Entry, error)          { return nil, nil }
func AddOrUpdate(path string) error   { return nil }
```

```bash
go test ./internal/wtstore/... -v
```
기대: 2 FAIL (round-trip 의 entries 비어 있음, AddOrUpdate 가 아무 효과 없음).

- [ ] **Step 3: Green — 구현**

`entry` 패키지의 포맷·flock 패턴을 그대로 활용한다. 파일 위치 초기화는 `xdgpath.StateDir("jg")` 와 동일한 폴더를 쓴다.

```go
package wtstore

import (
	"bufio"
	"fmt"
	"os"
	"path/filepath"
	"strconv"
	"strings"
	"syscall"
	"time"

	"github.com/silee-tools/jg/internal/xdgpath"
)

var DataFile string

func init() {
	stateDir := xdgpath.StateDir("jg")
	if stateDir != "" {
		_ = os.MkdirAll(stateDir, 0755)
		DataFile = filepath.Join(stateDir, "worktrees")
	} else {
		DataFile = filepath.Join(os.TempDir(), "jg-worktrees")
	}
}

type Entry struct {
	Path      string
	Rank      float64
	Timestamp int64
}

func parseLine(line string) (Entry, bool) {
	last := strings.LastIndex(line, "|")
	if last < 0 {
		return Entry{}, false
	}
	prev := strings.LastIndex(line[:last], "|")
	if prev < 0 {
		return Entry{}, false
	}
	rank, err := strconv.ParseFloat(line[prev+1:last], 64)
	if err != nil {
		return Entry{}, false
	}
	ts, err := strconv.ParseInt(line[last+1:], 10, 64)
	if err != nil {
		return Entry{}, false
	}
	return Entry{Path: line[:prev], Rank: rank, Timestamp: ts}, true
}

func formatLine(e Entry) string {
	return fmt.Sprintf("%s|%g|%d", e.Path, e.Rank, e.Timestamp)
}

func Load() ([]Entry, error) {
	f, err := os.Open(DataFile)
	if err != nil {
		if os.IsNotExist(err) {
			return nil, nil
		}
		return nil, err
	}
	defer f.Close()
	_ = syscall.Flock(int(f.Fd()), syscall.LOCK_SH)
	defer syscall.Flock(int(f.Fd()), syscall.LOCK_UN)

	var entries []Entry
	scanner := bufio.NewScanner(f)
	for scanner.Scan() {
		if e, ok := parseLine(strings.TrimSpace(scanner.Text())); ok {
			entries = append(entries, e)
		}
	}
	return entries, scanner.Err()
}

func Save(entries []Entry) error {
	tmp := DataFile + ".tmp"
	f, err := os.Create(tmp)
	if err != nil {
		return err
	}
	if err := syscall.Flock(int(f.Fd()), syscall.LOCK_EX); err != nil {
		f.Close()
		os.Remove(tmp)
		return err
	}
	w := bufio.NewWriter(f)
	for _, e := range entries {
		fmt.Fprintln(w, formatLine(e))
	}
	if err := w.Flush(); err != nil {
		f.Close()
		os.Remove(tmp)
		return err
	}
	syscall.Flock(int(f.Fd()), syscall.LOCK_UN)
	f.Close()
	return os.Rename(tmp, DataFile)
}

func AddOrUpdate(path string) error {
	entries, err := Load()
	if err != nil {
		return err
	}
	now := time.Now().Unix()
	for i, e := range entries {
		if e.Path == path {
			entries[i].Rank++
			entries[i].Timestamp = now
			return Save(entries)
		}
	}
	entries = append(entries, Entry{Path: path, Rank: 1, Timestamp: now})
	return Save(entries)
}
```

- [ ] **Step 4: Green 확인**

```bash
go test ./internal/wtstore/... -v
```
기대: 2 PASS.

- [ ] **Step 5: commit**

```bash
git add apps/jg/internal/wtstore
git commit -m "$(cat <<'EOF'
feat(jg): add worktree frecency store

XDG_STATE_HOME/jg/worktrees 파일에 worktree 별 (path, rank, timestamp) 를 저장한다.
파일 포맷·flock·atomic rename 패턴은 entry 패키지와 동일하다. jgw 점프가 본 store
와 entry store 양쪽을 동시에 가산할 때 사용된다.
EOF
)"
```

---

### Task 6: jgw fzf picker helper

**Files:**
- Create: `apps/jg/internal/fzf/jgwfzf.go`
- Create: `apps/jg/internal/fzf/jgwfzf_test.go`

- [ ] **Step 1: Red — preview command 빌더 테스트**

```go
// apps/jg/internal/fzf/jgwfzf_test.go
package fzf

import (
	"strings"
	"testing"
)

func TestWorktreePreviewCmdIncludesBranchAndLog(t *testing.T) {
	cmd := worktreePreviewCmd("/home/me")
	if !strings.Contains(cmd, "symbolic-ref") && !strings.Contains(cmd, "branch") {
		t.Errorf("preview cmd missing branch lookup: %q", cmd)
	}
	if !strings.Contains(cmd, "log -1") {
		t.Errorf("preview cmd missing log -1: %q", cmd)
	}
}
```

- [ ] **Step 2: Red 실행**

빈 시그니처 → 실패 확인:

```go
func worktreePreviewCmd(home string) string { return "" }
```

```bash
go test ./internal/fzf/... -run TestWorktreePreviewCmd -v
```

- [ ] **Step 3: Green — preview 빌더 + picker runner 구현**

```go
// jgwfzf.go
package fzf

import (
	"fmt"
	"os"
	"os/exec"
	"strings"
)

// WorktreePickerInput 은 jgw 의 fzf picker 호출 파라미터다.
type WorktreePickerInput struct {
	Candidates  []string // 후보 path 들 (현재 위치 제외)
	CurrentPath string   // 현재 위치한 worktree path. 비어 있으면 header-lines 미사용
	StepHeader  string   // "[1/2 repo 선택]" / "[2/2 worktree 선택]"
	OriginLine  string   // "원본: <path> (<branch>)" — 흐름 b 의 2단계에서만 채움
}

// RunWorktreePicker 는 fzf 를 띄우고 선택된 path 를 반환한다.
// 취소 시 빈 문자열을 반환한다.
func RunWorktreePicker(in WorktreePickerInput) (string, error) {
	fzfPath, err := exec.LookPath("fzf")
	if err != nil {
		return "", fmt.Errorf("fzf not found. Install it: brew install fzf")
	}
	home, _ := os.UserHomeDir()

	headerParts := []string{}
	if in.StepHeader != "" {
		headerParts = append(headerParts, in.StepHeader)
	}
	if in.OriginLine != "" {
		headerParts = append(headerParts, in.OriginLine)
	}
	header := strings.Join(headerParts, "\n")

	args := []string{
		"--height=40%",
		"--reverse",
		"--no-sort",
		"--select-1",
		"--keep-right",
		"--wrap",
		"--preview", worktreePreviewCmd(home),
	}
	if header != "" {
		args = append(args, "--header", header)
	}

	var input strings.Builder
	if in.CurrentPath != "" {
		fmt.Fprintln(&input, shortenPath(in.CurrentPath, home))
		args = append(args, "--header-lines=1")
	}
	for _, p := range in.Candidates {
		fmt.Fprintln(&input, shortenPath(p, home))
	}

	cmd := exec.Command(fzfPath, args...)
	cmd.Stderr = os.Stderr
	cmd.Stdin = strings.NewReader(input.String())
	out, err := cmd.Output()
	if err != nil {
		if exitErr, ok := err.(*exec.ExitError); ok {
			if exitErr.ExitCode() == 1 || exitErr.ExitCode() == 130 {
				return "", nil
			}
		}
		return "", err
	}
	return expandPath(strings.TrimSpace(string(out)), home), nil
}

// worktreePreviewCmd 는 focused 항목의 브랜치/마지막 커밋 제목/마지막 커밋 시각을
// 표시하는 preview 명령을 만든다.
func worktreePreviewCmd(home string) string {
	resolve := fmt.Sprintf(`p="{}"; p="${p/#\\~/%s}"`, home)
	return resolve + `; echo "branch: $(git -C \"$p\" symbolic-ref --short HEAD 2>/dev/null || echo \"(detached)\")"; echo; git -C "$p" log -1 --format='subject: %s%ndate:    %cd' --date=format:'%Y-%m-%d %H:%M' 2>/dev/null`
}
```

주의: `worktreePreviewCmd` 의 escape 가 까다롭다. shell 인 `{}` 가 fzf placeholder 임을 기억하고 단위 테스트로 핵심 토큰 존재를 검증하는 데 그친다. 실제 동작은 E2E 에서 검증.

- [ ] **Step 4: Green 확인**

```bash
go test ./internal/fzf/... -v
```
기대: 신규 + 기존 PASS.

- [ ] **Step 5: commit**

```bash
git add apps/jg/internal/fzf
git commit -m "$(cat <<'EOF'
feat(jg): add worktree-aware fzf picker helper

jgw 용 RunWorktreePicker 를 추가한다. step header + origin line 의 multi-line
header, 현재 위치를 비활성으로 띄우는 --header-lines=1, focused 항목에 대해
branch/last commit subject/date 를 보여주는 preview command 를 구성한다.
EOF
)"
```

---

### Task 7: jgw 흐름 a + b 본체

**Files:**
- Modify: `apps/jg/cmd/jg/main.go` (`runJgw` 본체)
- Create: `apps/jg/cmd/jg/jgw.go` (긴 함수 분리)
- Modify: `apps/jg/cmd/jg/main_test.go` (분기 결정 함수 테스트)

- [ ] **Step 1: Red — 흐름 분기 결정 함수 테스트**

```go
// main_test.go 에 추가
func TestJgwFlowDecision(t *testing.T) {
	// cwd 가 repo 안이고 인자 없으면 flowA
	if got := decideFlow(true, nil); got != flowA {
		t.Errorf("decideFlow(true, nil) = %v, want flowA", got)
	}
	// cwd 가 repo 안이지만 pattern 명시되면 flowB
	if got := decideFlow(true, []string{"pat"}); got != flowB {
		t.Errorf("decideFlow(true, [pat]) = %v, want flowB", got)
	}
	// cwd 가 repo 밖이면 flowB
	if got := decideFlow(false, nil); got != flowB {
		t.Errorf("decideFlow(false, nil) = %v, want flowB", got)
	}
}
```

- [ ] **Step 2: Red 실행 → 빈 시그니처 → 실패 관찰**

`apps/jg/cmd/jg/jgw.go` 신규 작성:

```go
package main

type flow int

const (
	flowA flow = iota
	flowB
)

func decideFlow(inRepo bool, args []string) flow { return flowA }
```

```bash
go test ./cmd/jg/... -run TestJgwFlowDecision -v
```
기대: 2 FAIL (flowB 두 케이스).

- [ ] **Step 3: Green — decideFlow 구현**

```go
func decideFlow(inRepo bool, args []string) flow {
	if !inRepo || len(args) > 0 {
		return flowB
	}
	return flowA
}
```

- [ ] **Step 4: Green 확인**

```bash
go test ./cmd/jg/... -v
```

- [ ] **Step 5: jgw 본체 작성**

`apps/jg/cmd/jg/jgw.go` 에 다음을 추가한다.

```go
import (
	"fmt"
	"os"
	"os/exec"
	"strings"

	"github.com/silee-tools/jg/internal/entry"
	"github.com/silee-tools/jg/internal/frecency"
	"github.com/silee-tools/jg/internal/fzf"
	"github.com/silee-tools/jg/internal/worktree"
	"github.com/silee-tools/jg/internal/wtstore"
)

func runJgwBody(args []string) {
	cwd, _ := os.Getwd()
	repoRoot, inRepo := detectRepoRoot(cwd)

	switch decideFlow(inRepo, args) {
	case flowA:
		runJgwFlowA(repoRoot, cwd)
	case flowB:
		runJgwFlowB(args, cwd)
	}
}

func detectRepoRoot(cwd string) (string, bool) {
	cmd := exec.Command("git", "-C", cwd, "rev-parse", "--show-toplevel")
	out, err := cmd.Output()
	if err != nil {
		return "", false
	}
	return strings.TrimSpace(string(out)), true
}

func runJgwFlowA(repoRoot, cwd string) {
	wts, err := worktree.List(repoRoot)
	if err != nil || len(wts) == 0 {
		fmt.Fprintln(os.Stderr, "jgw: no worktrees")
		os.Exit(1)
	}
	mainPath, mainBranch := splitMain(wts)
	candidates, current := splitCurrent(wts, cwd)
	if len(candidates) == 0 && current != "" {
		// 사용자가 유일한 worktree 안에 이미 있음 — 점프 없음
		os.Exit(0)
	}
	if len(candidates) == 0 {
		fmt.Println(mainPath)
		_ = entry.AddOrUpdate(mainPath)
		return
	}
	selected, err := fzf.RunWorktreePicker(fzf.WorktreePickerInput{
		Candidates:  candidates,
		CurrentPath: current,
		StepHeader:  "[1/1 worktree 선택]",
		OriginLine:  fmt.Sprintf("원본: %s (%s)", mainPath, mainBranch),
	})
	if err != nil || selected == "" {
		os.Exit(1)
	}
	fmt.Println(selected)
	_ = wtstore.AddOrUpdate(selected)
	_ = entry.AddOrUpdate(repoRoot)
}

func runJgwFlowB(args []string, cwd string) {
	entries, err := entry.Load()
	if err != nil || len(entries) == 0 {
		fmt.Fprintln(os.Stderr, "jgw: no repos in jg store")
		os.Exit(1)
	}
	query := strings.Join(args, " ")
	sorted := frecency.SortWithBoost(entries, query)
	paths := make([]string, 0, len(sorted))
	for _, e := range sorted {
		paths = append(paths, e.Path)
	}
	repoPicked, err := fzf.RunWorktreePicker(fzf.WorktreePickerInput{
		Candidates: paths,
		StepHeader: "[1/2 repo 선택]",
	})
	if err != nil || repoPicked == "" {
		os.Exit(1)
	}
	wts, err := worktree.List(repoPicked)
	if err != nil || len(wts) == 0 {
		fmt.Println(repoPicked)
		_ = entry.AddOrUpdate(repoPicked)
		return
	}
	mainPath, mainBranch := splitMain(wts)
	if len(wts) == 1 {
		fmt.Println(mainPath)
		_ = entry.AddOrUpdate(mainPath)
		return
	}
	candidates := make([]string, 0, len(wts))
	for _, w := range wts {
		candidates = append(candidates, w.Path)
	}
	current := ""
	selected, err := fzf.RunWorktreePicker(fzf.WorktreePickerInput{
		Candidates:  candidates,
		CurrentPath: current,
		StepHeader:  "[2/2 worktree 선택]",
		OriginLine:  fmt.Sprintf("원본: %s (%s)", mainPath, mainBranch),
	})
	if err != nil || selected == "" {
		os.Exit(1)
	}
	fmt.Println(selected)
	_ = wtstore.AddOrUpdate(selected)
	_ = entry.AddOrUpdate(repoPicked)
}

func splitMain(wts []worktree.Worktree) (path, branch string) {
	for _, w := range wts {
		if w.IsMain {
			return w.Path, w.Branch
		}
	}
	return "", ""
}

func splitCurrent(wts []worktree.Worktree, cwd string) (candidates []string, current string) {
	for _, w := range wts {
		if cwd == w.Path || strings.HasPrefix(cwd, w.Path+string(os.PathSeparator)) {
			current = w.Path
			continue
		}
		candidates = append(candidates, w.Path)
	}
	return
}
```

- [ ] **Step 6: `runJgw` 의 placeholder 본체를 진짜 본체로 교체**

`apps/jg/cmd/jg/main.go` 의 `runJgw` 끝부분:

```go
func runJgw(args []string) {
	if len(args) > 0 {
		switch args[0] {
		case "-v", "--version", "-version":
			fmt.Fprint(os.Stdout, versionLine("jgw", version))
			return
		case "-h", "--help", "-help":
			printJgwHelp()
			return
		}
	}
	runJgwBody(args)
}
```

- [ ] **Step 7: 빌드 + 단위 테스트**

```bash
cd apps/jg && go build ./... && go test ./...
```
기대: 빌드 성공, 모든 테스트 PASS.

- [ ] **Step 8: commit**

```bash
git add apps/jg/cmd/jg
git commit -m "$(cat <<'EOF'
feat(jg): implement jgw flow a/b dispatch and body

cwd 가 git repo 안이고 인자가 없으면 흐름 a(현 repo 의 worktree picker 한 단계),
그렇지 않으면 흐름 b(repo picker → worktree picker 두 단계) 로 분기한다. 선택 시
worktree store + repo store 양쪽 점수를 가산한다. linked worktree 가 0개이면
picker 를 띄우지 않고 즉시 원본으로 점프한다.
EOF
)"
```

---

### Task 8: shell init 출력에 jgw 함수 추가

**Files:**
- Modify: `apps/jg/internal/shell/shell.go`
- Modify: `apps/jg/internal/shell/shell_test.go` (없으면 신규)

- [ ] **Step 1: Red — init 출력에 `jgw()` 등장 검증 테스트**

```go
// apps/jg/internal/shell/shell_test.go (없으면 신규 작성)
package shell

import (
	"strings"
	"testing"
)

func TestInitZshIncludesJgwFunction(t *testing.T) {
	out := InitZsh()
	if !strings.Contains(out, "jgw()") {
		t.Errorf("InitZsh output missing jgw(): %s", out)
	}
}

func TestInitBashIncludesJgwFunction(t *testing.T) {
	out := InitBash()
	if !strings.Contains(out, "jgw()") {
		t.Errorf("InitBash output missing jgw(): %s", out)
	}
}
```

- [ ] **Step 2: Red 실행 → 실패 관찰**

```bash
go test ./internal/shell/... -v
```
기대: 2 FAIL.

- [ ] **Step 3: Green — shell.go 확장**

```go
// shell.go
package shell

import "fmt"

func InitZsh() string {
	return `# jg - frecency-based git repo jumper
_jg_chpwd() { command jg --add "$PWD" &! }
autoload -Uz add-zsh-hook
add-zsh-hook chpwd _jg_chpwd
jg() {
  local result
  result=$(command jg "$@")
  local ret=$?
  if [[ $ret -eq 0 && -d "$result" ]]; then
    builtin cd "$result"
  elif [[ -n "$result" ]]; then
    echo "$result"
  fi
  return $ret
}
jgw() {
  local result
  result=$(command jgw "$@")
  local ret=$?
  if [[ $ret -eq 0 && -d "$result" ]]; then
    builtin cd "$result"
  elif [[ -n "$result" ]]; then
    echo "$result"
  fi
  return $ret
}`
}

func InitBash() string {
	return `# jg - frecency-based git repo jumper
jg() {
  local result
  result=$(command jg "$@")
  local ret=$?
  if [[ $ret -eq 0 && -d "$result" ]]; then
    builtin cd "$result"
  elif [[ -n "$result" ]]; then
    echo "$result"
  fi
  return $ret
}
jgw() {
  local result
  result=$(command jgw "$@")
  local ret=$?
  if [[ $ret -eq 0 && -d "$result" ]]; then
    builtin cd "$result"
  elif [[ -n "$result" ]]; then
    echo "$result"
  fi
  return $ret
}
_jg_prompt_command() {
  if [[ "$_JG_PREV_PWD" != "$PWD" ]]; then
    _JG_PREV_PWD="$PWD"
    command jg --add "$PWD" &
  fi
}
PROMPT_COMMAND="_jg_prompt_command${PROMPT_COMMAND:+;$PROMPT_COMMAND}"`
}

func Init(shellName string) (string, error) {
	switch shellName {
	case "zsh":
		return InitZsh(), nil
	case "bash":
		return InitBash(), nil
	default:
		return "", fmt.Errorf("unsupported shell: %s (supported: zsh, bash)", shellName)
	}
}
```

- [ ] **Step 4: Green 확인**

```bash
go test ./internal/shell/... -v
```

- [ ] **Step 5: commit**

```bash
git add apps/jg/internal/shell
git commit -m "$(cat <<'EOF'
feat(jg): emit jgw shell function from jg init

jg init zsh / jg init bash 출력이 jg() 함수와 함께 jgw() 함수를 함께 정의한다.
사용자는 기존처럼 eval "$(jg init zsh)" 한 줄로 양쪽 함수를 모두 등록한다.
EOF
)"
```

---

### Task 9: 자동완성 4종

**Files:**
- Create: `apps/jg/completions/_jgw`
- Create: `apps/jg/completions/jgw.bash`
- Modify: `apps/jg/completions/_jg` (등록 경로 차집합 보완은 *생략* — 본 plan 의 범위 밖. 단순히 jg 자동완성 유지)
- Modify: `apps/jg/completions/jg.bash` (변경 없음, 그대로)

> 본 plan 은 jg 측 자동완성 개선을 별 PR 로 분리한다. 본 task 는 jgw 신규 자동완성만 추가한다.

- [ ] **Step 1: `_jgw` 작성**

```zsh
#compdef jgw

_jgw() {
  local -a repos
  repos=("${(@f)$(command jg -l 2>/dev/null | awk '{print $NF}')}")

  _arguments -C \
    {-v,--version}'[Show version]' \
    {-h,--help}'[Show help]' \
    '1: :->first_arg' \
    && return 0

  case "$state" in
    first_arg)
      compadd -a repos
      ;;
  esac
}

_jgw "$@"
```

- [ ] **Step 2: `jgw.bash` 작성**

```bash
_jgw() {
  local cur prev
  COMPREPLY=()
  cur="${COMP_WORDS[COMP_CWORD]}"
  prev="${COMP_WORDS[COMP_CWORD-1]}"

  if [[ $COMP_CWORD -eq 1 ]]; then
    case "$cur" in
      -*)
        COMPREPLY=( $(compgen -W "-h --help -v --version" -- "$cur") )
        ;;
      *)
        local repos
        repos=$(command jg -l 2>/dev/null | awk '{print $NF}')
        COMPREPLY=( $(compgen -W "$repos" -- "$cur") )
        ;;
    esac
  fi
}

complete -F _jgw jgw
```

- [ ] **Step 3: 수동 확인 — zsh 자동완성**

```bash
fpath=("$PWD/apps/jg/completions" $fpath) zsh -c 'autoload -U compinit && compinit && _jgw'
```
기대: 에러 없이 함수 정의 로드.

(실제 탭 보완 테스트는 E2E 에서 수행)

- [ ] **Step 4: commit**

```bash
git add apps/jg/completions/_jgw apps/jg/completions/jgw.bash
git commit -m "$(cat <<'EOF'
feat(jg): add jgw zsh and bash completions

jgw 의 첫 인자 자동완성으로 jg frecency store 의 repo 목록을 노출한다. 플래그는
-h/--help, -v/--version 만 보완한다. 본 저장소 "자동완성 최대 제공" 원칙의
첫 적용 사례.
EOF
)"
```

---

### Task 10: GoReleaser archive + Homebrew formula

**Files:**
- Modify: `apps/jg/.goreleaser.yaml` (이미 `files: [completions/*, plugin/*]` 가 있는지 확인)
- Modify: 별 저장소 `silee-tools/homebrew-tap` 의 `Formula/jg.rb`

- [ ] **Step 1: GoReleaser archive 확인**

`apps/jg/.goreleaser.yaml` 의 `archives` 블록을 점검한다. `files:` 에 `completions/*` 가 이미 포함되어 있다 (현재 파일 확인). 없으면 추가:

```yaml
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}-v{{ .Version }}-{{ .Os }}-{{ .Arch }}"
    files:
      - completions/*
      - plugin/*
```

이미 있으면 변경 없음.

- [ ] **Step 2: Homebrew formula 갱신 (별 저장소)**

`silee-tools/homebrew-tap` 저장소에서 `Formula/jg.rb` 의 `install` 블록을 다음으로 갱신:

```ruby
def install
  bin.install "jg"
  bin.install_symlink "jg" => "jgw"
  zsh_completion.install "completions/_jg"
  zsh_completion.install "completions/_jgw"
  bash_completion.install "completions/jg.bash" => "jg"
  bash_completion.install "completions/jgw.bash" => "jgw"
  (share/"jg"/"plugin").install "plugin/jg.plugin.zsh"
end
```

`caveats` 도 갱신:

```ruby
def caveats
  <<~EOS
    Run 'jg setup' to configure shell integration,
    then restart your shell or run: exec $SHELL

    jgw is installed alongside jg as a sibling command for jumping
    between git worktrees of the same repo.
  EOS
end
```

- [ ] **Step 3: 본 저장소 commit (homebrew-tap 변경은 별 PR)**

```bash
git add apps/jg/.goreleaser.yaml
# 변경 없으면 commit 생략
```

GoReleaser 가 이미 적절히 설정돼 있다면 본 task 의 본 저장소 측 변경은 없을 수 있다. homebrew-tap 변경만 별 PR 로 진행하고, 그 PR 링크를 본 PR 본문에 첨부한다.

---

### Task 11: README 갱신

**Files:**
- Modify: `README.md` 의 도구 표
- Modify: `docs/README_ko.md` 의 도구 표

- [ ] **Step 1: 루트 README 의 도구 표 갱신**

`README.md` 의 도구 표에서 `jg` 행을 다음으로 교체:

```markdown
| [jg / jgw](apps/jg/) | Go | Frecency-based CLI for quickly jumping to git repositories (jg) and to worktrees within a repo (jgw). |
```

- [ ] **Step 2: 한국어 README 의 도구 표 갱신**

`docs/README_ko.md` 에서도 동일한 톤으로 jg 행을 갱신한다(한국어 description 유지).

- [ ] **Step 3: commit**

```bash
git add README.md docs/README_ko.md
git commit -m "$(cat <<'EOF'
docs: list jg/jgw as a pair in tool table

jg 와 jgw 가 동일 바이너리 형제 명령임을 도구 표 한 행으로 분명히 표기한다.
EOF
)"
```

---

---

## 4. 1회성 E2E 검증 (TDD 강화 조항 2)

본 plan 의 모든 task 가 끝난 시점에 다음 5건의 수동 E2E 를 수행하고, 각 케이스의 동작 증거(셸 캡처) 를 PR 본문에 첨부한다.

1. **빈 worktree 케이스**
   - 임시 repo `repo-empty` 생성, linked worktree 추가 없이 그 안으로 cd
   - `jgw` 실행 → picker 없이 종료, `pwd` 변화 없음 또는 원본으로 점프 확인
2. **현 repo flow a — 2개 worktree**
   - linked worktree 2개를 가진 repo 안에서 `jgw` 실행
   - header 에 `[1/1 worktree 선택]` + `원본: <path> (<branch>)` 가 보임
   - 항목 선택 → `pwd` 가 선택한 path 로 변경
3. **repo 밖 flow b — 두 단계 picker**
   - cwd 를 `~` 같은 git repo 밖으로 옮긴 뒤 `jgw` 실행
   - 1단계 header `[1/2 repo 선택]` → repo 선택 → 2단계 header `[2/2 worktree 선택]` 확인
4. **header-lines 비활성 행**
   - 현재 worktree X 안에서 `jgw` 실행 → picker 맨 위에 X 가 dim/비활성으로 노출
   - 키보드 ↑ 로 cursor 가 그 행에 멈추지 않는 것을 확인
5. **preview 창 갱신**
   - 후보가 2개 이상인 picker 에서 ↑/↓ 로 focus 이동
   - preview 창의 `branch: ...`, `subject: ...`, `date: ...` 가 항목별로 달라지는 것을 확인

각 케이스의 통과 후 `jg -l` 와 worktree store 파일을 한 번 출력해 점수가 올라간 entry 가 양쪽에 모두 기록되었는지 확인한다.

---

## 5. PR 본문 템플릿

본 작업이 끝나 PR 을 만들 때, 본 저장소의 표준 템플릿 순서(Before → After → 무엇 → 어떻게 → 왜 → 검증) 를 따른다. 검증 절에는 위 E2E 5건의 증거를 첨부하고, 각 task 의 단위 테스트 실패/통과 숫자 쌍을 한 줄로 정리해 둔다.
